language: node_js
node_js: stable

# S: Build Lifecycle
install:
    - npm install

script:
    - hexo g

after_scrit:
    - cd ./public
    - git init
    - git config user.name "stanhe"
    - git config user.email "hshl4314@126.com"
    - git add .
    - git commit -m "publish"
    - git push --force --quiet "https://${GH_TOKEN}@{GH_REF}" master:master

branches:
    only:
        - blog_source

env:
    global:
        - GH_REF: github.com/stanhe/stanhe.github.io.git
